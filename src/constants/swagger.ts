import { version } from '../../package.json';

const TITLE = 'Superdough API';
const DESCRIPTION = 'The swagger docs for the Superdough API.';
const VERSION = version;

export default {
  TITLE,
  DESCRIPTION,
  VERSION,
};
